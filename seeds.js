var mongoose = require("mongoose");
    User = require("./models/user");

var seedData = [
{
  username:"admin",
  password:"password"
},
{
    username:"shankar",
    password:"passwd"
}
];

function seedDB(){
    //Create User
}